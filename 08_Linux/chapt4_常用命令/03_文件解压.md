# 1. 压缩解压

## 目录
- [1. 压缩解压](#1-压缩解压)
  - [目录](#目录)
  - [1.1 压缩与解压的基础概念](#11-压缩与解压的基础概念)
  - [1.2 使用 tar 命令打包与解压文件](#12-使用-tar-命令打包与解压文件)
  - [1.3 文件夹的打包与解压操作](#13-文件夹的打包与解压操作)
  - [1.4 基础命令总结](#14-基础命令总结)

---

## 1.1 压缩与解压的基础概念

比较常用的压缩和解压也是重点，我们在Windows中经常需要下载一些压缩包，并且将压缩包解压才能获得里面的文件，而Linux中也支持文件的压缩和解压。

## 1.2 使用 tar 命令打包与解压文件

这里我们使用`tar`命令来完成文件亚索和解压操作，在Linux中比较常用的是gzip格式，后缀名一般为.gz，tar命令的参数-c表示对文件进行压缩，创建新的压缩文件，-x表示进行解压操作，-z表示以gzip格式进行操作，-v可以在处理过程中输出一些日志信息，-f表示对普通文件进行操作，这里我们创建三个文件并对这三个文件进行打包：

```sh
# 将所有以.txt结尾的文件打包，并以gzip格式压缩为test.tar.gz，同时显示执行过程
tar -zcvf test.tar.gz *.txt

# 列出当前目录中的文件
ls

test@ubuntu-server:~$ tar -zcvf test.tar.gz *.txt
1.txt
2.txt
3.txt
test@ubuntu-server:~$ ls
1.txt  2.txt  3.txt  test.tar.gz
test@ubuntu-server:~$ 
```

接着我们删除刚刚三个文件，再执行解压操作，得到压缩包中文件：

```sh
# 删除所有以.txt结尾的文件
rm *.txt
# 列出当前目录中的文件
ls
# 以gzip格式解压test.tar.gz文件，并显示解压过程
tar -zxvf test.tar.gz 
# 列出当前目录中的文件
ls

test@ubuntu-server:~$ rm *.txt
test@ubuntu-server:~$ ls
test.tar.gz
test@ubuntu-server:~$ tar -zxvf test.tar.gz 
1.txt
2.txt
3.txt
test@ubuntu-server:~$ ls
1.txt  2.txt  3.txt  test.tar.gz
```

## 1.3 文件夹的打包与解压操作

同样的，我们也可以对一个文件夹进行打包：

```sh
# 将所有以.txt结尾的文件移动到test目录中
mv *.txt test

# 将test目录整体打包，并以gzip格式压缩为test.tar.gz
tar -zcvf test.tar.gz test/

# 递归删除test目录及其内容
rm -r test

# 列出当前目录中的文件
ls

# 以gzip格式解压test.tar.gz文件，并显示解压过程
tar -zxvf test.tar.gz 

# 列出当前目录中的文件
ls

# 查看test目录内的文件列表
ls test

test@ubuntu-server:~$ mv *.txt test
test@ubuntu-server:~$ tar -zcvf test.tar.gz test/
test/
test/1.txt
test/2.txt
test/3.txt

test@ubuntu-server:~$ rm -r test
test@ubuntu-server:~$ ls
test.tar.gz 
test@ubuntu-server:~$ tar -zxvf test.tar.gz 
test/
test/1.txt
test/2.txt
test/3.txt
test@ubuntu-server:~$ ls
test  test.tar.gz
test@ubuntu-server:~$ ls test
1.txt  2.txt  3.txt
```

## 1.4 基础命令总结

到此，Linux的一些基本命令就讲解为止。