# 1. 接口规则校验

## 目录
- [1. 接口规则校验](#1-接口规则校验)
  - [目录](#目录)
  - [1.1 传统手动校验方式](#11-传统手动校验方式)
  - [1.2 引入Validation框架进行接口校验](#12-引入validation框架进行接口校验)
  - [1.3 校验异常全局处理](#13-校验异常全局处理)
  - [1.4 常用验证注解说明](#14-常用验证注解说明)
  - [1.5 实体类对象参数校验](#15-实体类对象参数校验)
  - [1.6 处理JSON请求体(@RequestBody)的校验陷阱与DTO方案](#16-处理json请求体requestbody的校验陷阱与dto方案)
    - [1.6.1 陷阱：无法在 Map 内部直接校验](#161-陷阱无法在-map-内部直接校验)
    - [1.6.2 解决方案：引入 DTO 数据传输对象](#162-解决方案引入-dto-数据传输对象)

---

## 1.1 传统手动校验方式

通常我们在使用SpringMvc框架编写接口时，很有可能用户**发送的数据存在一些问题**，比如下面这个接口：

```java
@ResponseBody
@PostMapping("/submit")
public String submit(String username,
                     String password){
    System.out.println(username.substring(3));
    System.out.println(password.substring(2, 10));
    return "请求成功!";
}
```

这个接口中，我们需要将用户名和密码分割然后打印，在正常情况下，因为用户名长度规定不小于5，如果用户发送的数据是没有问题的，那么就可以正常运行，这也是我们所希望的情况，但是**如果用户发送的数据并不是按照规定的，那么就会直接报错：**

![image-20230716215850225](https://s2.loli.net/2023/07/16/n1FMADOiQCRcGw6.png)

这个时候，我们就需要在请求进来之前进行校验了，最简单的办法就是判断一下：

```java
@ResponseBody
@PostMapping("/submit")
public String submit(String username,
                     String password){
    if(username.length() > 3 && password.length() > 10) {
        System.out.println(username.substring(3));
        System.out.println(password.substring(2, 10));
        return "请求成功!";
    } else {
        return "请求失败";
    }
}
```

## 1.2 引入Validation框架进行接口校验

虽然这样就能直接解决问题，但是如果我们的每一个接口都需要这样去进行配置，那么是不是太麻烦了一点？SpringBoot为我们提供了很方便的**接口校验框架**：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

现在，我们可以直接使用注解完成全部接口的校验：

```java
@Slf4j
@Validated   //首先在Controller上开启接口校验
@Controller
public class TestController {

    ...

    @ResponseBody
    @PostMapping("/submit")
    public String submit(@Length(min = 3) String username,     /* username最短为3 */
                         @Length(min = 10) String password){  //password最短为10
        System.out.println(username.substring(3));
        System.out.println(password.substring(2, 10));
        return "请求成功!";
    }
}
```

现在，我们的接口校验就可以快速进行配置了，一个接口就能搞定：

![image-20230716220839816](https://s2.loli.net/2023/07/16/EibCc4sHWflywek.png)

## 1.3 校验异常全局处理

不过这样依然会抛出一个异常，对用户不太友好，我们可以稍微处理一下，这里我们可以直接使用之前在SSM阶段中学习的异常处理Controller来自行处理这类异常：

```java
@ControllerAdvice
public class ValidationController {

    @ResponseBody
    @ExceptionHandler(ConstraintViolationException.class)
    public String error(ValidationException e){
        return e.getMessage();   //出现异常直接返回消息
    }
}
```

![image-20230716221420324](https://s2.loli.net/2023/07/16/7JH6BzOhlUe9gkG.png)

## 1.4 常用验证注解说明

除了@Length之外，我们也可以使用其他的接口来实现各种数据校验：

| 验证注解及具体用法 | 验证的数据类型 | 说明 |
| :---: | :---: | :---: |
| @AssertFalse | Boolean,boolean | 值必须是false |
| @AssertTrue | Boolean,boolean | 值必须是true |
| @NotNull | 任意类型 | 值不能是null |
| @Null | 任意类型 | 值必须是null |
| @Min(value=1) | BigDecimal、BigInteger、byte、short、int、long、double 以及任何Number或CharSequence子类型 | 大于等于@Min指定的值 |
| @Max(value=100) | 同上 | 小于等于@Max指定的值 |
| @DecimalMin(value="0.01") | 同上 | 大于等于@DecimalMin指定的值（超高精度） |
| @DecimalMax(value="99.99") | 同上 | 小于等于@DecimalMax指定的值（超高精度） |
| @Digits(integer=3, fraction=2) | 同上 | 限制整数位数和小数位数上限 |
| @Size(min=1, max=10) | 字符串、Collection、Map、数组等 | 长度在指定区间之内，如字符串长度、集合大小等 |
| @Past | 如 java.util.Date, java.util.Calendar 等日期类型 | 值必须比当前时间早 |
| @Future | 同上 | 值必须比当前时间晚 |
| @NotBlank | CharSequence及其子类 | 值不为空，在比较时会去除字符串的首位空格 |
| @Length(min=3, max=20) | CharSequence及其子类 | 字符串长度在指定区间内 |
| @NotEmpty | CharSequence及其子类、Collection、Map、数组 | 值不为null且长度不为空（字符串长度不为0，集合大小不为0） |
| @Range(min=1, max=100) | BigDecimal、BigInteger、CharSequence、byte、short、int、long 以及原子类型和包装类型 | 值在指定区间内 |
| @Email | CharSequence及其子类 | 值必须是邮件格式 |
| @Pattern(regexp="^[a-zA-Z0-9]+$") | CharSequence及其子类 | 值需要与指定的正则表达式匹配 |
| @Valid | 任何非原子类型 | 用于验证对象属性 |

## 1.5 实体类对象参数校验

虽然这样已经很方便了，但是在遇到对象的时候，依然不太方便，比如：

```java
@Data
public class Account {
    String username;
    String password;
}
```

```java
@ResponseBody
@PostMapping("/submit")
public String submit(Account account){   //直接使用对象接收
    System.out.println(account.getUsername().substring(3));
    System.out.println(account.getPassword().substring(2, 10));
    return "请求成功!";
}
```

此时接口是以对象形式接收前端发送的表单数据的，这个时候就没办法向上面一样使用注解了，那么现在又该怎么做呢？

对应对象类型，我们也可以进行验证，方法如下：

```java
@ResponseBody
@PostMapping("/submit")  //先在将实体类作为参数的方法上添加@Valid注解 表示需要验证
public String submit(@Valid Account account){
    System.out.println(account.getUsername().substring(3));
    System.out.println(account.getPassword().substring(2, 10));
    return "请求成功!";
}
```

```java
@Data
public class Account {
    @Length(min = 3)   //再在实体类定义里对需要校验的数据添加注解
    String username;
    @Length(min = 10)
    String password;
}
```

这样当受到请求时，就会对对象中的字段进行校验了，这里我们稍微修改一下ValidationController的错误处理，对于实体类接收参数的验证，会抛出MethodArgumentNotValidException异常，这里也进行一下处理：

```java
@ResponseBody
@ExceptionHandler({ConstraintViolationException.class, MethodArgumentNotValidException.class})
public String error(Exception e){
    if(e instanceof ConstraintViolationException exception) {
        return exception.getMessage();
    } else if(e instanceof MethodArgumentNotValidException exception){
        if (exception.getFieldError() == null) return "未知错误";
        return exception.getFieldError().getDefaultMessage();
    }
    return "未知错误";
}
```

这样就可以正确返回对应的错误信息了。

## 1.6 处理JSON请求体(@RequestBody)的校验陷阱与DTO方案

在前后端分离开发中，前端通常以 JSON 格式提交数据，后端接口会使用 `@RequestBody` 注解来接收。为了图方便，有时会直接使用 `Map` 接收数据，但这会导致**无法进行参数校验**。

### 1.6.1 陷阱：无法在 Map 内部直接校验

如果接口定义如下，试图接收 JSON 数据：

```java
@PostMapping("/login")
public Result login(@RequestBody Map<String, String> loginData) {
    // 此时无法使用 @Length 等注解校验 loginData 里面的 username 和 password
    String username = loginData.get("username");
    String password = loginData.get("password"); 
    // ...
}
```

校验注解（如 `@Length`、`@NotBlank`）必须作用于 Java 类的具体属性（字段）上，或者直接作用于控制器的方法参数上。它们**不能**自动穿透到 `Map` 的键值对内部进行校验。

### 1.6.2 解决方案：引入 DTO 数据传输对象

标准的规范做法是：**创建一个 DTO（Data Transfer Object）类专门用于接收该请求的数据，并在该类的属性上添加校验注解。**

**第一步：创建对应的 DTO 类**

```java
import lombok.Data;
import org.hibernate.validator.constraints.Length;
import jakarta.validation.constraints.NotBlank;

@Data // 使用 Lombok 自动生成 Getter/Setter
public class LoginDTO {

    @NotBlank(message = "用户名不能为空")
    @Length(min = 3, message = "用户名长度不能少于3位")
    private String username;

    @NotBlank(message = "密码不能为空")
    private String password;
}
```

**第二步：在 Controller 中使用 @Validated 开启校验**

将原来接收参数的 `Map` 替换为新创建的 `LoginDTO`，并且在参数前面加上 `@Validated`（或 `@Valid`）注解。

```java
@PostMapping("/login")
// 使用 @Validated 开启针对 LoginDTO 的校验
public Result login(@RequestBody @Validated LoginDTO loginData) {
    
    // 校验通过后，直接通过 Getter 获取数据
    String username = loginData.getUsername();
    String password = loginData.getPassword(); 

    // ... 执行后续业务逻辑
    return Result.success("登录成功"); 
}
```

使用这种方式后，如果前端传入的 `username` 长度小于 3，Spring Boot 会抛出 `MethodArgumentNotValidException` 异常。配合前面 **1.5节** 中配置好的全局异常处理器（`ValidationController`），前端就能直接接收到我们在注解中配置的 `message` 错误提示（例如 "用户名长度不能少于3位"）。